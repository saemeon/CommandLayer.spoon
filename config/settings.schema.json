{
  "$comment": "Generated by `lua tests/reference.lua`; do not edit.",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "additionalProperties": false,
  "description": "settings.json, over the Spoon's config/defaults.jsonc.",
  "patternProperties": {
    "^(?!(?:ambient|applescript|appmenus|apps|audio|brew|browser|calculator|chooser|codicons|context|developer|editor|files|finder|frecency|git|github|grep|hammerspoon|help|icons|keepawake|keybindings|lazygit|maccy|media|popup|projects|quicklinks|raycast|relevance|secureinput|selection|settings|shell|shortcuts|snippets|spoon|ssh|system|systemsettings|tasks|terminal|url|urlhandler|vscode|vscodebridge|webview|windows|workbench|zoxide)\\.)[A-Za-z0-9_-]+\\.": {
      "description": "A setting of an extension, presenter or ranker that does not ship with the Spoon."
    }
  },
  "properties": {
    "$schema": {
      "description": "Where an editor finds this file: the Spoon's config/settings.schema.json.",
      "type": "string"
    },
    "actionsView": {
      "default": "actions",
      "description": "The picker cmd+k opens for the highlighted row.",
      "type": "string"
    },
    "aliases": {
      "additionalProperties": {
        "items": {
          "type": "string"
        },
        "type": [
          "string",
          "array"
        ]
      },
      "description": "Aliases of your own, a string or a list, by command id: { \"windows.leftHalf\": \"lh\" }. Typed as the whole query, ignoring case, an alias puts its command's row first, whatever the rankers score.",
      "type": "object"
    },
    "ambient.enabled": {
      "default": true,
      "description": "Whether Ambient is on; a change applies on reload. The app in front, the clipboard, this Mac's name and whether it is online, as context keys.",
      "type": "boolean"
    },
    "ambient.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "ambient.privateTypes": {
      "default": [
        "org.nspasteboard.ConcealedType",
        "org.nspasteboard.TransientType",
        "org.nspasteboard.AutoGeneratedType",
        "com.agilebits.onepassword",
        "de.petermaurer.TransientPasteboardType",
        "com.typeit4me.clipping",
        "Pasteboard generator type"
      ],
      "description": "Pasteboard types that keep what was copied out of the context: a password, a one-time code",
      "type": "array"
    },
    "appearance": {
      "additionalProperties": false,
      "default": {
        "actionRows": 7,
        "actionWidth": 24,
        "dark": true,
        "maxRows": 200,
        "rows": 12,
        "width": 36
      },
      "description": "What every picker shares. A presenter's own options are its settings.",
      "properties": {
        "actionRows": {
          "default": 7,
          "description": "How many rows the cmd+k panel shows at once.",
          "type": "number"
        },
        "actionWidth": {
          "default": 24,
          "description": "The cmd+k panel's width, in percent of the screen's.",
          "type": "number"
        },
        "dark": {
          "default": true,
          "description": "Dark pickers.",
          "type": "boolean"
        },
        "maxRows": {
          "default": 200,
          "description": "The most rows a picker is handed; everything is still matched and ranked, only drawing is capped.",
          "type": "number"
        },
        "rows": {
          "default": 12,
          "description": "How many rows a picker shows at once.",
          "type": "number"
        },
        "width": {
          "default": 36,
          "description": "A picker's width, in percent of the screen's.",
          "type": "number"
        }
      },
      "type": "object"
    },
    "applescript.enabled": {
      "default": true,
      "description": "Whether AppleScript is on; a change applies on reload. Run an AppleScript.",
      "type": "boolean"
    },
    "applescript.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "appmenus.cacheSeconds": {
      "default": 30,
      "description": "How long an app's menus are kept before reading them again",
      "type": "integer"
    },
    "appmenus.enabled": {
      "default": true,
      "description": "Whether App menus is on; a change applies on reload.",
      "type": "boolean"
    },
    "appmenus.maxDepth": {
      "default": 3,
      "description": "How deep to follow submenus",
      "type": "integer"
    },
    "appmenus.maxItems": {
      "default": 400,
      "description": "At most this many menu items from one app",
      "type": "integer"
    },
    "appmenus.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "appmenus.shortcuts": {
      "default": true,
      "description": "Each menu item's keyboard shortcut in its subtitle",
      "type": "boolean"
    },
    "appmenus.skipMenus": {
      "default": [
        "Apple",
        "Services"
      ],
      "description": "Top-level menus not listed",
      "type": "array"
    },
    "apps.confirmQuit": {
      "default": true,
      "description": "Ask before quitting an app",
      "type": "boolean"
    },
    "apps.enabled": {
      "default": true,
      "description": "Whether Apps is on; a change applies on reload.",
      "type": "boolean"
    },
    "apps.ignoreApps": {
      "default": [
        "Hammerspoon",
        "loginwindow"
      ],
      "description": "Apps never recorded as used",
      "type": "array"
    },
    "apps.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "apps.mruLimit": {
      "default": 30,
      "description": "How many apps to remember having used",
      "type": "integer"
    },
    "apps.searchPaths": {
      "default": [
        "/Applications",
        "/System/Applications",
        "~/Applications",
        "/System/Library/CoreServices/Applications",
        "/Applications/Xcode.app/Contents/Applications"
      ],
      "description": "The folders apps are listed from",
      "type": "array"
    },
    "audio.enabled": {
      "default": true,
      "description": "Whether Audio is on; a change applies on reload. Switch the sound output and input device, and mute the microphones.",
      "type": "boolean"
    },
    "audio.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "brew.enabled": {
      "default": true,
      "description": "Whether Homebrew is on; a change applies on reload. brew update, upgrade, outdated, doctor, cleanup, install, search and info, in the terminal.",
      "type": "boolean"
    },
    "brew.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "browser.bookmarks": {
      "default": true,
      "description": "Bookmarks",
      "type": "boolean"
    },
    "browser.enabled": {
      "default": true,
      "description": "Whether Browser is on; a change applies on reload.",
      "type": "boolean"
    },
    "browser.history": {
      "default": true,
      "description": "History",
      "type": "boolean"
    },
    "browser.historyLimit": {
      "default": 300,
      "description": "How many pages of history are read from each browser",
      "type": "integer"
    },
    "browser.historySeconds": {
      "default": 120,
      "description": "Seconds between reading history and bookmarks again",
      "type": "integer"
    },
    "browser.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "browser.recentTabs": {
      "default": 10,
      "description": "How many tabs the recent picker lists",
      "type": "integer"
    },
    "browser.searchURL": {
      "default": "https://search.brave.com/search?q=${query}",
      "description": "Where searching the web goes; ${query} is what you typed",
      "type": "string"
    },
    "browser.tabOrder": {
      "default": "recent",
      "description": "Order tabs by",
      "enum": [
        "recent",
        "browser"
      ],
      "enumDescriptions": [
        "When each was last active, from the browser's session file",
        "The browser's own order"
      ],
      "type": "string"
    },
    "browser.tabs": {
      "default": true,
      "description": "Open tabs",
      "type": "boolean"
    },
    "calculator.enabled": {
      "default": true,
      "description": "Whether Calculator is on; a change applies on reload.",
      "type": "boolean"
    },
    "calculator.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "chooser.fg": {
      "description": "Title colour, as hs.drawing.color takes one",
      "type": "object"
    },
    "chooser.match": {
      "description": "Colour of what you typed",
      "type": "object"
    },
    "chooser.screen": {
      "default": "primary",
      "description": "Open on display",
      "enum": [
        "primary",
        "focused"
      ],
      "enumDescriptions": [
        "The main display, with the menu bar",
        "The display with the focused window"
      ],
      "type": "string"
    },
    "chooser.styled": {
      "default": true,
      "description": "Highlight what you typed, with a smaller subtitle",
      "type": "boolean"
    },
    "chooser.styledRows": {
      "default": 40,
      "description": "How many rows from the top are styled; each is made again per keystroke",
      "type": "integer"
    },
    "chooser.subText": {
      "description": "Subtitle colour",
      "type": "object"
    },
    "chooser.subtitleSize": {
      "default": 12,
      "description": "Subtitle size, in points",
      "type": "number"
    },
    "chooser.titleSize": {
      "default": 17,
      "description": "Title size, in points",
      "type": "number"
    },
    "codicons.enabled": {
      "default": true,
      "description": "Whether Codicons is on; a change applies on reload. VS Code's $(icon) names, drawn from the codicon font.",
      "type": "boolean"
    },
    "codicons.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "context.enabled": {
      "default": true,
      "description": "Whether Context is on; a change applies on reload.",
      "type": "boolean"
    },
    "context.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "defaultView": {
      "default": "root",
      "description": "The picker entering the layer opens.",
      "type": "string"
    },
    "developer.enabled": {
      "default": true,
      "description": "Whether Developer is on; a change applies on reload. Show the log and set how much reaches it, see where the time goes, and inspect the context.",
      "type": "boolean"
    },
    "developer.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "editor.enabled": {
      "default": true,
      "description": "Whether Editor is on; a change applies on reload. Open a file or folder in the editor you have.",
      "type": "boolean"
    },
    "editor.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "files.enabled": {
      "default": true,
      "description": "Whether Files is on; a change applies on reload.",
      "type": "boolean"
    },
    "files.fileLimit": {
      "default": 25,
      "description": "At most this many recent files, before anything is typed",
      "type": "integer"
    },
    "files.fileWindowDays": {
      "default": 7,
      "description": "How many days back a file counts as recently used",
      "type": "integer"
    },
    "files.iconRows": {
      "default": 20,
      "description": "At most this many icons loaded for one list of files; icons shared by a file type are kept",
      "type": "integer"
    },
    "files.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "files.searchLimit": {
      "default": 200,
      "description": "At most this many files found by typing",
      "type": "integer"
    },
    "files.searchRoot": {
      "default": "~",
      "description": "The folder file search looks in",
      "type": "string"
    },
    "finder.enabled": {
      "default": true,
      "description": "Whether Finder is on; a change applies on reload.",
      "type": "boolean"
    },
    "finder.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "frecency.dayWeight": {
      "default": 2,
      "description": "How much a pick within the last day counts",
      "order": 2,
      "type": "number"
    },
    "frecency.hourWeight": {
      "default": 4,
      "description": "How much a pick within the last hour counts",
      "order": 1,
      "type": "number"
    },
    "frecency.maxAge": {
      "default": 10000,
      "description": "Past this total, every rank is scaled down and the smallest forgotten",
      "type": "number"
    },
    "frecency.olderWeight": {
      "default": 0.25,
      "description": "How much an older pick counts",
      "order": 4,
      "type": "number"
    },
    "frecency.weekWeight": {
      "default": 0.5,
      "description": "How much a pick within the last week counts",
      "order": 3,
      "type": "number"
    },
    "git.cloneRoots": {
      "description": "Folders offered to clone into; ~ is your home folder",
      "type": "array"
    },
    "git.confirmDestructive": {
      "default": true,
      "description": "Ask before deleting a branch or dropping a stash",
      "type": "boolean"
    },
    "git.confirmPush": {
      "default": true,
      "description": "Ask before pushing",
      "type": "boolean"
    },
    "git.enabled": {
      "default": true,
      "description": "Whether Git is on; a change applies on reload.",
      "type": "boolean"
    },
    "git.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "git.searchLimit": {
      "default": 25,
      "description": "The most repositories gh finds for a clone",
      "type": "integer"
    },
    "github.cacheSeconds": {
      "default": 300,
      "description": "How long gh's answers are kept, in seconds",
      "type": "integer"
    },
    "github.enabled": {
      "default": false,
      "description": "Whether GitHub is on; a change applies on reload.",
      "type": "boolean"
    },
    "github.issues": {
      "default": true,
      "description": "Open issues assigned to you",
      "type": "boolean"
    },
    "github.limit": {
      "default": 40,
      "description": "The most of each gh lists",
      "type": "integer"
    },
    "github.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "github.pullRequests": {
      "default": true,
      "description": "Your open pull requests",
      "type": "boolean"
    },
    "github.repos": {
      "default": true,
      "description": "Your repositories",
      "type": "boolean"
    },
    "grep.enabled": {
      "default": true,
      "description": "Whether Text search is on; a change applies on reload. Text in a folder or a file, searched with ripgrep as you type.",
      "type": "boolean"
    },
    "grep.maxColumns": {
      "default": 200,
      "description": "A longer line found is shown cut to this many characters",
      "type": "integer"
    },
    "grep.maxPerFile": {
      "default": 5,
      "description": "At most this many lines found in one file",
      "type": "integer"
    },
    "grep.maxResults": {
      "default": 200,
      "description": "At most this many lines found, in all",
      "type": "integer"
    },
    "grep.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "grep.roots": {
      "description": "Folders Search Text in… offers first",
      "type": "array"
    },
    "hammerspoon.enabled": {
      "default": true,
      "description": "Whether Hammerspoon is on; a change applies on reload. Reload Hammerspoon, open its console, preferences and config.",
      "type": "boolean"
    },
    "hammerspoon.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "help.enabled": {
      "default": true,
      "description": "Whether Help is on; a change applies on reload. Every picker there is a way to, and how, in the ? picker.",
      "type": "boolean"
    },
    "help.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "icons.enabled": {
      "default": true,
      "description": "Whether Icons is on; a change applies on reload.",
      "type": "boolean"
    },
    "icons.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "keepawake.display": {
      "default": true,
      "description": "Keep the display awake too, not only the machine",
      "type": "boolean"
    },
    "keepawake.durations": {
      "default": [
        15,
        60,
        120
      ],
      "description": "The minutes Keep awake for… offers",
      "type": "array"
    },
    "keepawake.enabled": {
      "default": true,
      "description": "Whether Keep Awake is on; a change applies on reload. Keep the Mac awake through caffeinate, and say what else is keeping it awake.",
      "type": "boolean"
    },
    "keepawake.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "keybindings.enabled": {
      "default": true,
      "description": "Whether Keyboard Shortcuts is on; a change applies on reload. Every keybinding in effect, in the Keyboard Shortcuts picker, removed or changed in keybindings.json.",
      "type": "boolean"
    },
    "keybindings.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "lazygit.enabled": {
      "default": true,
      "description": "Whether lazygit is on; a change applies on reload. Open a project or folder in lazygit, in the terminal.",
      "type": "boolean"
    },
    "lazygit.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "logLevel": {
      "default": "warning",
      "description": "How much the layer writes to the Hammerspoon console, in VS Code's level names.",
      "enum": [
        "off",
        "trace",
        "debug",
        "info",
        "warning",
        "error"
      ],
      "type": "string"
    },
    "maccy.enabled": {
      "default": false,
      "description": "Whether Maccy is on; a change applies on reload.",
      "type": "boolean"
    },
    "maccy.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "maccy.popupChord": {
      "default": "cmd+shift+c",
      "description": "The chord that opens Maccy, when its own preferences cannot be read",
      "type": "string"
    },
    "matchers": {
      "default": [
        "fzf",
        "substring"
      ],
      "description": "The matchers used, in the order tried: the first that takes a query decides which rows stay, and one not named does not run.",
      "items": {
        "enum": [
          "fzf",
          "substring"
        ],
        "type": "string"
      },
      "type": "array"
    },
    "media.enabled": {
      "default": true,
      "description": "Whether Media is on; a change applies on reload. Play/pause, next and previous track in whatever is playing, as the media keys do.",
      "type": "boolean"
    },
    "media.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "performance": {
      "additionalProperties": false,
      "default": {
        "keep": 200,
        "slowMilliseconds": 250
      },
      "description": "Where the time goes: a line per open and keystroke at debug, every span at trace, and \"Developer: Show Performance\".",
      "properties": {
        "keep": {
          "default": 200,
          "description": "How many of the latest spans are kept.",
          "type": "number"
        },
        "slowMilliseconds": {
          "default": 250,
          "description": "A span or a whole open taking this long is a warning; 0 records nothing.",
          "type": "number"
        }
      },
      "type": "object"
    },
    "popup.depth": {
      "default": 3,
      "description": "How many levels of submenus are built before the menu opens",
      "type": "integer"
    },
    "popup.limit": {
      "default": 40,
      "description": "The most rows a menu shows",
      "type": "integer"
    },
    "prefixes": {
      "additionalProperties": {
        "items": {
          "type": "string"
        },
        "type": [
          "string",
          "array"
        ]
      },
      "description": "Prefixes of your own, added to a picker's by its name: { \"recent\": \"r \" }.",
      "type": "object"
    },
    "presenter": {
      "default": "chooser",
      "description": "The presenter drawing a picker that names none.",
      "enum": [
        "chooser",
        "popup"
      ],
      "type": "string"
    },
    "projects.cacheSeconds": {
      "default": 300,
      "description": "How long a project scan is kept, in seconds",
      "type": "integer"
    },
    "projects.enabled": {
      "default": true,
      "description": "Whether Projects is on; a change applies on reload.",
      "type": "boolean"
    },
    "projects.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "projects.onlyGitRepos": {
      "default": true,
      "description": "Only directories that are git repositories",
      "type": "boolean"
    },
    "projects.opensWith": {
      "default": "editor.open",
      "description": "The command that opens a project, given its path as target",
      "type": "string"
    },
    "projects.projectRoots": {
      "default": [
        "~/projects",
        "~/work",
        "~/dev"
      ],
      "description": "Where a scan looks for projects; ~ is your home folder",
      "type": "array"
    },
    "projects.projectSource": {
      "default": "auto",
      "description": "Where projects come from",
      "enum": [
        "auto",
        "zoxide",
        "z",
        "find"
      ],
      "enumDescriptions": [
        "Whichever of the others is available, in that order",
        "Directories you actually visit, ranked by zoxide's frecency",
        "The older ~/.z database",
        "A scan of the project roots for .git directories"
      ],
      "type": "string"
    },
    "projects.remoteProjects": {
      "default": true,
      "description": "VS Code's recent folders on SSH hosts, opened in a remote window",
      "type": "boolean"
    },
    "projects.scanDepth": {
      "default": 3,
      "description": "How deep a scan descends looking for a .git directory",
      "type": "integer"
    },
    "projects.useEditorRecents": {
      "default": true,
      "description": "VS Code's own recent folders ahead of the rest",
      "type": "boolean"
    },
    "quicklinks.enabled": {
      "default": true,
      "description": "Whether Quicklinks is on; a change applies on reload.",
      "type": "boolean"
    },
    "quicklinks.links": {
      "default": [
        {
          "title": "Search GitHub",
          "url": "https://github.com/search?q=${query}"
        },
        {
          "title": "Search GitHub repositories",
          "url": "https://github.com/search?type=repositories&q=${query}"
        },
        {
          "title": "Search npm",
          "url": "https://www.npmjs.com/search?q=${query}"
        },
        {
          "title": "Search MDN",
          "url": "https://developer.mozilla.org/en-US/search?q=${query}"
        },
        {
          "title": "Search Hammerspoon docs",
          "url": "https://www.hammerspoon.org/search.html?q=${query}"
        },
        {
          "title": "Search Stack Overflow",
          "url": "https://stackoverflow.com/search?q=${query}"
        }
      ],
      "description": "Links: { title, url, prefix }; a ${query} in the URL takes what you type",
      "type": "array"
    },
    "quicklinks.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "rankers": {
      "additionalProperties": false,
      "default": {
        "alphabetical": 0,
        "frecency": 0.2,
        "relevance": 1,
        "specificity": 0.35
      },
      "description": "Each ranker's weight. Every ranker scores a row 0..1, so its weight is the whole of how much it counts; one at 0 records nothing.",
      "properties": {
        "alphabetical": {
          "type": "number"
        },
        "frecency": {
          "description": "How often and how lately you pick a row, as zoxide ranks folders",
          "type": "number"
        },
        "relevance": {
          "type": "number"
        },
        "specificity": {
          "type": "number"
        }
      },
      "type": "object"
    },
    "raycast.commands": {
      "default": [
        {
          "title": "Raycast: Clipboard history",
          "url": "raycast://extensions/raycast/clipboard-history/clipboard-history"
        },
        {
          "title": "Raycast: Search files",
          "url": "raycast://extensions/raycast/file-search/search-files"
        }
      ],
      "description": "Raycast deeplinks: { title, url }",
      "type": "array"
    },
    "raycast.enabled": {
      "default": false,
      "description": "Whether Raycast is on; a change applies on reload.",
      "type": "boolean"
    },
    "raycast.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "relevance.exactMatchBoost": {
      "default": 0,
      "description": "0..1: how much more a row counts when its label is exactly what was typed",
      "type": "number"
    },
    "secureinput.enabled": {
      "default": true,
      "description": "Whether Secure Input is on; a change applies on reload. Notice when macOS's Secure Input would drop the keys a command presses, and say which app holds it.",
      "type": "boolean"
    },
    "secureinput.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "selection.enabled": {
      "default": true,
      "description": "Whether Selection is on; a change applies on reload. The text selected in the app in front, read through Accessibility: Look Up, search, copy.",
      "type": "boolean"
    },
    "selection.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "settings.enabled": {
      "default": true,
      "description": "Whether Settings is on; a change applies on reload. Every extension and the switches each declares, flipped in the settings picker.",
      "type": "boolean"
    },
    "settings.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "shell.enabled": {
      "default": true,
      "description": "Whether Shell is on; a change applies on reload. Run a command line.",
      "type": "boolean"
    },
    "shell.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "shortcuts.enabled": {
      "default": true,
      "description": "Whether Shortcuts is on; a change applies on reload.",
      "type": "boolean"
    },
    "shortcuts.limit": {
      "default": 30,
      "description": "How many shortcuts are rows",
      "type": "integer"
    },
    "shortcuts.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "snippets.enabled": {
      "default": true,
      "description": "Whether Snippets is on; a change applies on reload.",
      "type": "boolean"
    },
    "snippets.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "spoon.enabled": {
      "default": true,
      "description": "Whether Spoons is on; a change applies on reload. Call another Spoon, or open a fallback when it is not loaded.",
      "type": "boolean"
    },
    "spoon.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "ssh.enabled": {
      "default": true,
      "description": "Whether SSH is on; a change applies on reload. The hosts in ~/.ssh/config: ssh in the terminal, or a remote window in the editor.",
      "type": "boolean"
    },
    "ssh.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "system.enabled": {
      "default": true,
      "description": "Whether System is on; a change applies on reload. Open and reveal things, Open With, lock the screen, macOS's Emoji & Symbols and its screenshots.",
      "type": "boolean"
    },
    "system.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "systemsettings.cacheSeconds": {
      "default": 3600,
      "description": "How long the list of panes is kept before it is read again, in seconds",
      "type": "integer"
    },
    "systemsettings.enabled": {
      "default": true,
      "description": "Whether System Settings is on; a change applies on reload.",
      "type": "boolean"
    },
    "systemsettings.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "tasks.enabled": {
      "default": true,
      "description": "Whether Tasks is on; a change applies on reload. The tasks in tasks.json beside settings.json, run in the terminal.",
      "type": "boolean"
    },
    "tasks.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "terminal.application": {
      "default": "terminal",
      "description": "The terminal a folder opens in and a command runs in",
      "enum": [
        "terminal",
        "wezterm",
        "kitty",
        "alacritty"
      ],
      "enumDescriptions": [
        "Terminal.app; running a command needs Hammerspoon allowed to control it, in System Settings > Privacy & Security > Automation",
        "WezTerm: wezterm cli spawn while it runs, else started through open",
        "kitty",
        "Alacritty"
      ],
      "type": "string"
    },
    "terminal.enabled": {
      "default": true,
      "description": "Whether Terminal is on; a change applies on reload. Open a folder, or run a command line, in the terminal you choose.",
      "type": "boolean"
    },
    "terminal.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "terminal.shell": {
      "default": "",
      "description": "The shell a command runs in, as a login shell; empty for $SHELL",
      "type": "string"
    },
    "tools": {
      "additionalProperties": false,
      "default": {
        "loginShell": true,
        "paths": {},
        "use": {}
      },
      "description": "Where tools are, and which provider does a job.",
      "properties": {
        "loginShell": {
          "default": true,
          "description": "Whether start asks a login shell once for tools the known paths missed.",
          "type": "boolean"
        },
        "paths": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Where a tool is: { \"code\": \"/opt/local/bin/code\" }.",
          "type": "object"
        },
        "use": {
          "additionalProperties": {
            "type": "string"
          },
          "description": "Which provider does a job: { \"editor\": \"zed\" }.",
          "type": "object"
        }
      },
      "type": "object"
    },
    "url.enabled": {
      "default": true,
      "description": "Whether URL is on; a change applies on reload. A typed URL: open it, or cmd+k for Open with…, Copy URL and the other link verbs.",
      "type": "boolean"
    },
    "url.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "urlhandler.allowedCommands": {
      "description": "The command ids a hammerspoon://commandlayer?command=<id>&args=<JSON> URL may run; any web page can open such a URL",
      "type": "array"
    },
    "urlhandler.enabled": {
      "default": true,
      "description": "Whether URL Handler is on; a change applies on reload. Run a command from a hammerspoon://commandlayer URL.",
      "type": "boolean"
    },
    "urlhandler.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "useDefaultProfile": {
      "additionalProperties": false,
      "default": {
        "keybindings": false
      },
      "description": "In a profile other than default: what it takes from the default profile, as VS Code's \"Use Default Profile\" does.",
      "properties": {
        "keybindings": {
          "description": "The default profile's keybindings.json, before this profile's own.",
          "type": "boolean"
        }
      },
      "type": "object"
    },
    "views": {
      "description": "Pickers, merged by name: an entry changes the fields it names, and a new name adds one.",
      "items": {
        "additionalProperties": false,
        "description": "A picker: how it is reached, where its rows come from, and a few flags. Declared in settings.json's views; an extension feeds its menus.",
        "properties": {
          "answers": {
            "description": "Whether typing offers answers and switches on a prefix; true when absent, false in the cmd+k panel.",
            "type": "boolean"
          },
          "command": {
            "description": "A command taking text -- an input with fromQuery -- the picker holds alone: its row, with what is typed filled in.",
            "type": "string"
          },
          "compact": {
            "description": "Drawn at appearance's actionWidth and actionRows.",
            "type": "boolean"
          },
          "debounce": {
            "description": "In a search picker, the seconds typing pauses before a search; 0.15 when absent.",
            "type": "number"
          },
          "description": {
            "description": "What the picker is for.",
            "type": "string"
          },
          "empty": {
            "description": "What it says when it has nothing to show, filled like placeholder.",
            "type": "string"
          },
          "enabled": {
            "description": "false removes the picker.",
            "type": "boolean"
          },
          "icon": {
            "description": "A ThemeIcon, $(name), for its row.",
            "type": "string"
          },
          "kind": {
            "description": "list narrows the rows it gathered as you type; search asks the extensions' search hooks again instead; item offers the verbs on the subject it is given, as cmd+k does. list when absent.",
            "enum": [
              "list",
              "search",
              "item"
            ],
            "type": "string"
          },
          "menus": {
            "description": "The menus its rows come from.",
            "items": {
              "type": "string"
            },
            "type": "array"
          },
          "minQuery": {
            "description": "In a search picker, the fewest characters that search; 2 when absent.",
            "type": "number"
          },
          "name": {
            "description": "The picker's name, which keybindings, prefixes and sections refer to.",
            "type": "string"
          },
          "parent": {
            "description": "The picker it is a row in, reading Title ▸.",
            "type": "string"
          },
          "pickerRows": {
            "description": "Makes every picker with a prefix a row too, which types the prefix.",
            "type": "boolean"
          },
          "placeholder": {
            "description": "The search field's placeholder, filled from the context.",
            "type": "string"
          },
          "prefix": {
            "description": "Typed text starting with this opens the picker; a list gives several.",
            "items": {
              "type": "string"
            },
            "type": [
              "string",
              "array"
            ]
          },
          "presenter": {
            "description": "The presenter that draws it; the profile's presenter when absent.",
            "enum": [
              "chooser",
              "popup"
            ],
            "type": "string"
          },
          "recentlyUsed": {
            "description": "Puts this many rows picked most lately first, also while typing.",
            "type": "number"
          },
          "sections": {
            "description": "What it opens on before anything is typed: a few rows from each, then the rest.",
            "items": {
              "additionalProperties": false,
              "description": "A few rows from one extension or picker, shown before anything is typed, the first labelled. It needs from or view.",
              "properties": {
                "exclude": {
                  "description": "Extensions whose rows are left out before the limit is taken.",
                  "items": {
                    "type": "string"
                  },
                  "type": "array"
                },
                "from": {
                  "description": "The extension whose rows it takes, in the order the extension gave them.",
                  "type": "string"
                },
                "limit": {
                  "description": "How many rows it takes.",
                  "type": "number"
                },
                "title": {
                  "description": "The label on its first row; the extension's display name when absent.",
                  "type": "string"
                },
                "view": {
                  "description": "The picker whose opening rows it takes, instead.",
                  "type": "string"
                }
              },
              "type": "object"
            },
            "type": "array"
          },
          "textCommands": {
            "description": "Offers every command that takes text, with what was typed filled in: fallback when nothing matched; first while text is typed, ranked by use, before the picker's own rows that match. None when absent.",
            "enum": [
              "fallback",
              "first"
            ],
            "type": "string"
          },
          "title": {
            "description": "Names the picker in its parent, in the breadcrumb and in ?.",
            "type": "string"
          }
        },
        "required": [
          "name"
        ],
        "type": "object"
      },
      "type": "array"
    },
    "vscode.cacheSeconds": {
      "default": 60,
      "description": "How long the workspace storage's folders are kept, in seconds",
      "type": "integer"
    },
    "vscode.enabled": {
      "default": true,
      "description": "Whether VS Code is on; a change applies on reload.",
      "type": "boolean"
    },
    "vscode.limit": {
      "default": 40,
      "description": "Recent folders read from VS Code's workspace storage",
      "type": "integer"
    },
    "vscode.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "vscodebridge.enabled": {
      "default": false,
      "description": "Whether VS Code bridge is on; a change applies on reload. Commands answered by the Command Layer extension in VS Code; switch this on once it is installed.",
      "type": "boolean"
    },
    "vscodebridge.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "webview.enabled": {
      "default": true,
      "description": "Whether Web view is on; a change applies on reload. Show a page, some HTML or a file in a window of its own.",
      "type": "boolean"
    },
    "webview.height": {
      "default": 70,
      "description": "Window height, as a percentage of the screen",
      "type": "number"
    },
    "webview.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "webview.width": {
      "default": 60,
      "description": "Window width, as a percentage of the screen",
      "type": "number"
    },
    "windows.animationDuration": {
      "default": 0,
      "description": "Seconds a window takes to move",
      "type": "number"
    },
    "windows.enabled": {
      "default": true,
      "description": "Whether Windows is on; a change applies on reload.",
      "type": "boolean"
    },
    "windows.layouts": {
      "default": [
        {
          "icon": "$(screen-full)",
          "title": "Maximise",
          "unit": [
            0,
            0,
            1,
            1
          ]
        },
        {
          "icon": "$(layout-sidebar-left)",
          "title": "Left half",
          "unit": [
            0,
            0,
            0.5,
            1
          ]
        },
        {
          "icon": "$(layout-sidebar-right)",
          "title": "Right half",
          "unit": [
            0.5,
            0,
            0.5,
            1
          ]
        },
        {
          "title": "Top half",
          "unit": [
            0,
            0,
            1,
            0.5
          ]
        },
        {
          "title": "Bottom half",
          "unit": [
            0,
            0.5,
            1,
            0.5
          ]
        },
        {
          "title": "Left third",
          "unit": [
            0,
            0,
            0.33333333333333331,
            1
          ]
        },
        {
          "title": "Middle third",
          "unit": [
            0.33333333333333331,
            0,
            0.33333333333333331,
            1
          ]
        },
        {
          "title": "Right third",
          "unit": [
            0.66666666666666663,
            0,
            0.33333333333333331,
            1
          ]
        },
        {
          "title": "Left two thirds",
          "unit": [
            0,
            0,
            0.66666666666666663,
            1
          ]
        },
        {
          "title": "Top left",
          "unit": [
            0,
            0,
            0.5,
            0.5
          ]
        },
        {
          "title": "Top right",
          "unit": [
            0.5,
            0,
            0.5,
            0.5
          ]
        },
        {
          "title": "Bottom left",
          "unit": [
            0,
            0.5,
            0.5,
            0.5
          ]
        },
        {
          "title": "Bottom right",
          "unit": [
            0.5,
            0.5,
            0.5,
            0.5
          ]
        },
        {
          "title": "Centre",
          "unit": [
            0.15,
            0.1,
            0.7,
            0.8
          ]
        }
      ],
      "description": "Window layouts: a title, and a unit of x, y, width and height in fractions of the screen, which another provider takes by the title's name where it has that layout; applies on reload",
      "type": "array"
    },
    "windows.limit": {
      "default": 50,
      "description": "At most this many windows listed",
      "type": "integer"
    },
    "windows.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "windows.provider": {
      "default": "hammerspoon",
      "description": "What moves windows: a move the tool has no action for is not offered, and nothing is offered while the tool is not installed; applies on reload",
      "enum": [
        "hammerspoon",
        "raycast",
        "rectangle",
        "yabai",
        "aerospace"
      ],
      "enumDescriptions": [
        "Hammerspoon's own hs.window: every layout, other screens and full screen",
        "Raycast's Window Management, by deeplink",
        "Rectangle, by its URL scheme; no full screen",
        "yabai -m window: its grid for layouts, other displays and native full screen",
        "AeroSpace: other monitors and native full screen, no layouts"
      ],
      "type": "string"
    },
    "windows.source": {
      "default": "ordered",
      "description": "Which windows are listed, read as a picker lists them, once per open; windows.sourceByPicker chooses another for a picker",
      "enum": [
        "all",
        "ordered",
        "visible"
      ],
      "enumDescriptions": [
        "Every window on screen and every window of the apps in the Dock, minimised or on another Space, front to back; about 40 ms",
        "hs.window.orderedWindows(): visible standard windows in their own front-to-back order; about 40 ms",
        "hs.window.visibleWindows(): visible windows, front to back; about 20 ms"
      ],
      "type": "string"
    },
    "windows.sourceByPicker": {
      "default": {
        "recent": "all"
      },
      "description": "A source for a picker, by the picker's name -- { \"root\": \"ordered\", \"recent\": \"all\" } -- each all, ordered or visible; a picker not named uses windows.source",
      "type": "object"
    },
    "workbench.autoReload": {
      "default": true,
      "description": "Reload when settings.json or keybindings.json changes",
      "type": "boolean"
    },
    "workbench.enabled": {
      "default": true,
      "description": "Whether Preferences is on; a change applies on reload. Open the settings and keybindings files, switch profile, list problems, and forget what was picked.",
      "type": "boolean"
    },
    "workbench.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "zoxide.cacheSeconds": {
      "default": 60,
      "description": "How long zoxide's list is kept before asking again, in seconds",
      "type": "integer"
    },
    "zoxide.enabled": {
      "default": true,
      "description": "Whether zoxide is on; a change applies on reload. Folders you visit, ranked by zoxide; folders opened here are added to it.",
      "type": "boolean"
    },
    "zoxide.learn": {
      "default": true,
      "description": "Tell zoxide about folders opened here",
      "type": "boolean"
    },
    "zoxide.limit": {
      "default": 15,
      "description": "Folders listed in files before anything is typed",
      "type": "integer"
    },
    "zoxide.menus": {
      "additionalProperties": {
        "type": "boolean"
      },
      "description": "Which pickers its rows and commands are in, over what it declares: a menu's name to true (added) or false (taken out); a menu not named keeps the declaration. A command that is no row anywhere stays none.",
      "type": "object"
    },
    "zoxide.searchLimit": {
      "default": 50,
      "description": "The most folders found by typing",
      "type": "integer"
    }
  },
  "title": "Command Layer settings",
  "type": "object"
}
